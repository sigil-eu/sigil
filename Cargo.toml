[package]
name = "sigil-protocol"
version = "0.1.5"
edition = "2021"
description = "SIGIL — Sovereign Identity-Gated Interaction Layer. A modular security protocol for identity binding, optional content redaction, and tamper-evident audit in any networked caller-executor interaction."
license = "EUPL-1.2"
# Commercial licence available — see LICENSE-COMMERCIAL or https://sigil-protocol.org
repository = "https://github.com/sigil-eu/sigil"
homepage = "https://sigil-protocol.org"
documentation = "https://docs.rs/sigil-protocol"
readme = "README.md"
keywords = ["security", "identity", "agent", "mcp", "protocol"]
categories = ["authentication", "cryptography"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
async-trait = "0.1"
anyhow = "1"
uuid = { version = "1", features = ["v4"] }
ed25519-dalek = { version = "2", features = ["rand_core"] }
rand_core = { version = "0.6", features = ["getrandom"] }
base64ct = { version = "1", features = ["alloc"] }
hex = "0.4"
# Runtime deps for the standalone MCP server binary
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
regex = "1"
# Optional: live DID resolution + community pattern fetching from a SIGIL Registry
reqwest = { version = "0.12", features = ["json", "rustls-tls"], default-features = false, optional = true }
tracing = { version = "0.1", optional = true }

[[bin]]
name = "sigil-mcp-server"
path = "src/bin/sigil-mcp-server.rs"

[features]
# Enable RemoteScanner + verify_with_registry() — requires network access
registry = ["dep:reqwest", "dep:tracing"]

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
hmac  = "0.12"
sha2  = "0.10"
regex = "1"
